interface:
  display_name: "Humanizer"
  short_description: "Rewrite robotic prose into natural, author-faithful language."
  default_prompt: "Use $humanizer to rewrite this draft from its meaning, not by swapping synonyms, while preserving its facts and authorial voice."
