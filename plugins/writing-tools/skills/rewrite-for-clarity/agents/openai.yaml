interface:
  display_name: "Rewrite for Clarity"
  short_description: "Rewrite text naturally and clearly without changing meaning."
  default_prompt: "Use $rewrite-for-clarity to rewrite this around its intended message, not its original sentence frames, while preserving meaning and facts."
