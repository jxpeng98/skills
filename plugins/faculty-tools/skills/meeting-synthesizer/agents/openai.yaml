interface:
  display_name: "Meeting Synthesizer"
  short_description: "Record university meeting decisions and action owners."
  default_prompt: "Use $meeting-synthesizer to turn this university meeting record into traceable decisions, action owners, deadlines, and points needing confirmation."
