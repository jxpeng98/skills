# Humanizer Optimization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Strengthen `humanizer` into a safer, more concrete writing coach and polisher for general writing and academic writing.

**Architecture:** Keep `plugins/writing-tools/skills/humanizer/SKILL.md` as the canonical source and sync the generated Hermes mirror under `skills/humanizer/SKILL.md`. Add tests that assert the new workflow sections, safety boundaries, output contracts, and metadata version bump.

**Tech Stack:** Markdown skill files, JSON plugin metadata, Python `unittest`, existing `scripts/sync_hermes_tap.py`, existing `scripts/validate_plugins.py`.

---

## Source Insights

Use these as design inputs, not copied content:

- `DadaNanjesha/AI-Text-Humanizer-App`: optional transformation controls, academic/formal modes, before/after stats.
- `imsv1301/unmask-ai`: multi-pass shape can be adapted as `rewrite -> audit -> final fix`; do not adopt detector-bypass goals or bypass-rate reporting.
- `coinluu/humanize-ai-text-skill`: AI-feel diagnosis, protected facts, content-type recognition, platform adaptation, output modes, risk notes, and anti-over-humanization.
- `forint573/miAI-Humanizer-Skill-Awesome`: preserve facts and voice, remove process bleed, reject "clean but empty" writing, never invent proof.
- `bahayonghang/academic-writing-skills`: post-writing focus, module routing, source-aware output contracts, citation/claim preservation, readability rather than detector evasion.
- `PaperDebugger/paperdebugger` and `PaperMentor`: actionable comments and human-author control; comments should teach or guide rather than silently replace authorship.

Safety boundary: never implement detector bypass, fake imperfection, Turnitin/GPTZero targeting, bypass scores, or advice that helps evade academic integrity systems.

## File Structure

- Modify: `tests/test_faculty_tools.py`
  - Add tests that lock the new humanizer sections and version metadata.
- Modify: `plugins/writing-tools/skills/humanizer/SKILL.md`
  - Add the concrete workflow sections and output contracts.
- Modify: `plugins/writing-tools/.codex-plugin/plugin.json`
  - Bump `version` from `0.1.3` to `0.1.4`; update description/default prompts if needed.
- Modify: `plugins/writing-tools/.claude-plugin/plugin.json`
  - Bump `version` from `0.1.3` to `0.1.4`.
- Modify: `plugins/writing-tools/skillsplace.json`
  - Bump `version` from `0.1.3` to `0.1.4`; add tags if useful.
- Modify: `README.md`
  - Tighten the `humanizer` description if the skill surface changes.
- Generated by sync: `skills/humanizer/SKILL.md`
  - Do not edit directly; run `python3 scripts/sync_hermes_tap.py`.

## Task 1: Add Failing Tests For New Humanizer Contract

**Files:**
- Modify: `tests/test_faculty_tools.py`

- [ ] **Step 1: Add tests that assert the new workflow sections**

Add this test method to `FacultyToolsTests`:

```python
    def test_humanizer_has_preflight_diagnosis_and_audit_contract(self) -> None:
        text = (
            ROOT
            / "plugins"
            / "writing-tools"
            / "skills"
            / "humanizer"
            / "SKILL.md"
        ).read_text(encoding="utf-8")

        required_phrases = [
            "## Preflight Diagnosis",
            "## AI-Feel Diagnosis",
            "## Protected Facts",
            "## Edit Intensity",
            "## Post-Edit Audit",
            "light",
            "standard",
            "deep",
            "process bleed",
            "clean but empty",
            "No detector-evasion objective",
            "What was preserved",
        ]
        for phrase in required_phrases:
            with self.subTest(phrase=phrase):
                self.assertIn(phrase, text)
```

- [ ] **Step 2: Add tests that assert academic section-aware checks**

Add this test method:

```python
    def test_humanizer_has_section_aware_academic_checks(self) -> None:
        text = (
            ROOT
            / "plugins"
            / "writing-tools"
            / "skills"
            / "humanizer"
            / "SKILL.md"
        ).read_text(encoding="utf-8")

        required_phrases = [
            "## Section-Aware Academic Checks",
            "Introduction",
            "Body Paragraph",
            "Report Methods",
            "Results or Findings",
            "Conclusion",
            "claim-evidence fit",
            "author decision needed",
        ]
        for phrase in required_phrases:
            with self.subTest(phrase=phrase):
                self.assertIn(phrase, text)
```

- [ ] **Step 3: Add tests that assert writing-tools version bump**

Add this test method:

```python
    def test_writing_tools_version_bumped_for_humanizer_contract(self) -> None:
        paths = [
            ROOT / "plugins" / "writing-tools" / ".codex-plugin" / "plugin.json",
            ROOT / "plugins" / "writing-tools" / ".claude-plugin" / "plugin.json",
            ROOT / "plugins" / "writing-tools" / "skillsplace.json",
        ]

        for path in paths:
            with self.subTest(path=path.as_posix()):
                self.assertIn('"version": "0.1.4"', path.read_text(encoding="utf-8"))
```

- [ ] **Step 4: Run test to verify it fails**

Run:

```bash
python3 -m unittest tests/test_faculty_tools.py
```

Expected: FAIL because `humanizer` does not yet contain the new sections and metadata is still `0.1.3`.

## Task 2: Update Canonical Humanizer Skill

**Files:**
- Modify: `plugins/writing-tools/skills/humanizer/SKILL.md`

- [ ] **Step 1: Add `Preflight Diagnosis` after `Direction Selection`**

Insert this section:

```markdown
## Preflight Diagnosis

Before editing, identify:

- Direction: `General Writing Direction` or `Academic Writing Direction`
- Mode: `Coach Mode` or `Polisher Mode`
- Edit intensity: `light`, `standard`, or `deep`
- Goal and desired result
- Audience, channel, and stakes
- Text type and section type
- Claims, evidence, citations, data, terminology, and conclusions that must not change
- Whether the task is assessed academic work

If any item materially changes the edit, use `grill-me` before rewriting.
```

- [ ] **Step 2: Add `AI-Feel Diagnosis`**

Insert:

```markdown
## AI-Feel Diagnosis

Use this as a writing-quality diagnosis, not as detector evasion.

Look for:

- Macro opening that says little
- Template structure that hides the writer's actual point
- Mechanical transitions
- Abstract noun stacks
- Over-neutral or over-formal tone
- Repeated sentence shapes
- Process bleed from prompts, outlines, planning notes, or previous drafts
- Clean but empty prose that is polished but does not say a specific thing
- Generic claims without concrete context

No detector-evasion objective: do not mention AI scores, bypass rates, Turnitin,
GPTZero, Originality.ai, or similar systems as success criteria.
```

- [ ] **Step 3: Add `Protected Facts`**

Insert:

```markdown
## Protected Facts

Preserve these exactly unless the user explicitly asks to change them:

- Numbers, dates, names, course titles, institution names, and assignment terms
- Citations, source references, authors, years, quotations, and page numbers
- Research questions, variables, methods, datasets, sample sizes, and findings
- Claims, caveats, limitations, conclusions, and disciplinary terminology
- Legal, medical, financial, policy, grade, or assessment details

If a protected fact appears wrong, flag it as "needs verification" instead of
silently correcting or replacing it.
```

- [ ] **Step 4: Add `Edit Intensity`**

Insert:

```markdown
## Edit Intensity

- `light`: fix grammar, sentence flow, wordiness, and local clarity only.
- `standard`: improve paragraph flow, transitions, emphasis, and register while
  preserving structure.
- `deep`: restructure sentences or paragraph order for clarity. Use only when
  the user permits deeper changes. Report what was preserved and what changed.

Default to `standard` for ordinary polish and `light` for assessed academic work
unless the user asks for deeper coaching.
```

- [ ] **Step 5: Add `Post-Edit Audit`**

Insert:

```markdown
## Post-Edit Audit

After rewriting, check:

- What was preserved: claims, evidence, citations, data, methods, conclusions,
  and key terminology
- What changed: structure, transitions, sentence flow, word choice, and tone
- Whether any claim became stronger or broader than the source supports
- Whether any missing evidence, citation, or author decision is still needed
- Whether the edit stayed within the selected direction, mode, and intensity
```

- [ ] **Step 6: Add `Section-Aware Academic Checks`**

Insert:

```markdown
## Section-Aware Academic Checks

- `Introduction`: thesis, scope, motive, roadmap, and why the issue matters.
- `Body Paragraph`: topic sentence, claim-evidence fit, analysis, transition,
  and paragraph closer.
- `Report Methods`: procedure clarity, variables, materials, limits, and
  reproducibility.
- `Results or Findings`: result-first phrasing, evidence boundary, comparison,
  and no unsupported interpretation.
- `Discussion`: interpretation, limitation, implication, and connection back to
  the question.
- `Conclusion`: synthesis, answer to the original task, and no new unsupported
  claim.

When a section has a gap, label it as `author decision needed` instead of
inventing content.
```

- [ ] **Step 7: Update `Workflow` to include the new pass order**

Change the workflow so the order is:

```markdown
1. Run Preflight Diagnosis.
2. Choose direction, mode, and edit intensity.
3. Identify Protected Facts.
4. Run AI-Feel Diagnosis for writing-quality issues.
5. For academic writing, run Section-Aware Academic Checks.
6. Rewrite or coach according to the selected mode.
7. Run Post-Edit Audit before final output.
```

- [ ] **Step 8: Update `Output` contracts**

Add this output shape:

```markdown
For Coach Mode, include:

- Preflight Diagnosis
- AI-Feel Diagnosis
- Revision Priorities
- Suggested Revision or Targeted Excerpts
- Why These Changes Help
- What was preserved
- Author decision needed
- Academic integrity note when relevant

For Polisher Mode, include:

- Revised text
- Key edits made
- What was preserved
- Meaning, evidence, or citation items needing confirmation
```

- [ ] **Step 9: Run tests to verify the content passes**

Run:

```bash
python3 -m unittest tests/test_faculty_tools.py
```

Expected: FAIL only on metadata version until Task 3 is complete, or PASS if metadata was already updated.

## Task 3: Update Writing-Tools Metadata

**Files:**
- Modify: `plugins/writing-tools/.codex-plugin/plugin.json`
- Modify: `plugins/writing-tools/.claude-plugin/plugin.json`
- Modify: `plugins/writing-tools/skillsplace.json`
- Modify: `README.md`

- [ ] **Step 1: Bump version to `0.1.4`**

Change every writing-tools metadata file from:

```json
"version": "0.1.3"
```

to:

```json
"version": "0.1.4"
```

- [ ] **Step 2: Update Codex plugin description**

In `plugins/writing-tools/.codex-plugin/plugin.json`, set:

```json
"description": "Writing skills for clarity, tone, humanization, general writing polish, academic writing coaching, protected-fact preservation, and summarization."
```

Update `interface.longDescription` to:

```json
"Reusable writing skills for improving clarity, preserving meaning, coaching and polishing general or academic writing, protecting claims and evidence, making text sound more natural, and summarizing source material."
```

Add this `defaultPrompt` entry:

```json
"Diagnose and polish this academic paragraph while preserving my claims and evidence."
```

- [ ] **Step 3: Update README humanizer description**

Set the `humanizer` row to:

```markdown
| `humanizer` | Coach or polish general and academic writing with preflight diagnosis, protected-fact preservation, edit-intensity control, and academic integrity boundaries. |
```

- [ ] **Step 4: Run focused tests**

Run:

```bash
python3 -m unittest tests/test_faculty_tools.py
```

Expected: PASS.

## Task 4: Sync Hermes Mirror

**Files:**
- Generated: `skills/humanizer/SKILL.md`

- [ ] **Step 1: Sync mirror**

Run:

```bash
python3 scripts/sync_hermes_tap.py
```

Expected output:

```text
Synced 16 skills into skills
```

- [ ] **Step 2: Check mirror**

Run:

```bash
python3 scripts/sync_hermes_tap.py --check
```

Expected output:

```text
Hermes tap sync check passed
```

## Task 5: Full Validation

**Files:**
- No edits.

- [ ] **Step 1: Run full test discovery**

Run:

```bash
python3 -m unittest discover -s tests
```

Expected: all tests pass.

- [ ] **Step 2: Validate plugin layout**

Run:

```bash
python3 scripts/validate_plugins.py
```

Expected output:

```text
Plugin validation passed
```

- [ ] **Step 3: Check whitespace**

Run:

```bash
git diff --check
```

Expected: no output and exit code 0.

- [ ] **Step 4: Review repository boundary**

Run:

```bash
find . -name marketplace.json -print
```

Expected: no output.

Run:

```bash
rg -n "Turnitin|GPTZero|Originality.ai|bypass rate|detector-evasion objective|evade|avoid AI detector" plugins/writing-tools/skills/humanizer/SKILL.md README.md
```

Expected: matches only in safety-boundary language such as `No detector-evasion objective`; no success criteria or bypass instructions.

## Task 6: Commit And Optional Patch Release

**Files:**
- No edits unless validation requires fixes.

- [ ] **Step 1: Inspect changes**

Run:

```bash
git status --short --branch
git diff --stat
```

Expected: only the intended test, humanizer, metadata, README, and mirror changes.

- [ ] **Step 2: Commit**

Run:

```bash
git add tests/test_faculty_tools.py plugins/writing-tools/.codex-plugin/plugin.json plugins/writing-tools/.claude-plugin/plugin.json plugins/writing-tools/skills/humanizer/SKILL.md plugins/writing-tools/skillsplace.json README.md skills/humanizer/SKILL.md
git commit -m "feat(writing-tools): strengthen humanizer writing workflow"
```

- [ ] **Step 3: Patch release if requested**

If the user asks to release after implementation, tag `v0.2.1`:

```bash
git tag -a v0.2.1 -m "v0.2.1"
git push origin main
git push origin v0.2.1
```

Then watch the release workflow:

```bash
gh run list --workflow release.yml --limit 5
gh run watch <run-id> --exit-status
```

Expected: release workflow succeeds and uploads updated `writing-tools` assets.

## Self-Review

- Spec coverage: covers repository research synthesis, safety boundary, `humanizer` structure, tests, metadata, mirror sync, validation, and optional release.
- Placeholder scan: no unresolved placeholder markers or unspecified implementation steps.
- Type and path consistency: all file paths match the current repository layout and the existing test framework.
