interface:
  display_name: "Systematic Debugging"
  short_description: "Find root causes with a tight, evidence-led loop."
  default_prompt: "Use $systematic-debugging to reproduce this failure, identify its root cause, and verify the smallest justified fix."
